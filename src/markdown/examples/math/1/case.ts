import { Token } from "@transpiler/mod.ts";

const tokens: Token[] = [
  {
    children: [
      {
        id: "Math",
        value: "a+b=1",
      },
    ],
    id: "Paragraph",
  },
];

export default tokens;